package cmd

// Identifiers
const (
	CLI_NAME     = "skysqlcli"
	PROJECT_NAME = "skysql"
)

// Actions
const (
	GET    = "get"
	CREATE = "create"
	DELETE = "delete"
	UPDATE = "update"
)

// Entities
const (
	DATABASE = "database"
)
